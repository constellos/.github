name: Reusable CI Pipeline

on:
  workflow_call:
    inputs:
      node_version:
        type: string
        default: '22'
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN:
        required: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "lint step"
